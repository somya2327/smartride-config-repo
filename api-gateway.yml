server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      enabled: true   # 🔴 REQUIRED (FOR ACTUATOR)

      httpclient:
        connect-timeout: 3000
        response-timeout: 3s

      default-filters:
        - name: RequestRateLimiter
          args:
            redis-rate-limiter.replenishRate: 10
            redis-rate-limiter.burstCapacity: 20

      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**

        - id: ride-service
          uri: lb://ride-service
          predicates:
            - Path=/api/rides/**

        - id: driver-service
          uri: lb://driver-service
          predicates:
            - Path=/api/drivers/**

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8089/realms/smartride

  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoint:
    gateway:
      enabled: true   # 🔴 THIS IS THE KEY FIX

  endpoints:
    web:
      exposure:
        include: health,info,gateway
